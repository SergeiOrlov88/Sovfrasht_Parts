# ─── Секреты и локальная конфигурация ────────────────────────────────────────
# Файлы с ключами, оставленные в корне проекта: попадали бы в репозиторий при
# `git add -A`, потому что под шаблоны ниже не подходят.
*API*.txt
*АПИ*.txt
*ключ*.txt
*key*.txt
.env
.env.*
!.env.example
*.pem
*.key
*.p12
secrets/
credentials.json

# ─── Python (backend API, worker распознавания) ──────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
.venv/
env/
ENV/
build/
dist/
*.egg-info/
.eggs/
pip-wheel-metadata/

# тесты и типы
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.hypothesis/

# Celery / RQ
celerybeat-schedule
celerybeat.pid
dump.rdb

# ─── Node / React + Vite (PWA-клиент) ────────────────────────────────────────
node_modules/
.pnp
.pnp.js
dist/
dist-ssr/
build/
.vite/
*.local

# логи менеджеров пакетов
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# кэши инструментов
.eslintcache
.stylelintcache
.parcel-cache/
.turbo/

# Service Worker генерируется при сборке PWA — в репозиторий не кладём
sw.js
workbox-*.js

# ─── Пользовательские загрузки и медиа ───────────────────────────────────────
# Сканы деталей, фото с камеры — это данные, а не код
uploads/
media/
storage/
tmp/
temp/

# ─── ML: модели и веса распознавания ─────────────────────────────────────────
# Веса тяжёлые и версионируются отдельно (реестр моделей / S3).
# Пути привязаны к корню репозитория: иначе правило поглощает backend/app/models —
# слой ORM-моделей, который обязан быть в Git.
/models/
/weights/
*.pt
*.pth
*.onnx
*.h5
*.pkl
*.safetensors

# ─── Docker / инфраструктура ─────────────────────────────────────────────────
docker-compose.override.yml
docker-compose.*.local.yml
.docker/

# ─── Базы данных (локальные дампы) ───────────────────────────────────────────
*.sqlite
*.sqlite3
*.db
*.sql.gz
*.dump
pgdata/
postgres-data/

# ─── Логи ────────────────────────────────────────────────────────────────────
logs/
*.log

# ─── IDE и редакторы ─────────────────────────────────────────────────────────
.idea/
.vscode/
!.vscode/extensions.json
*.swp
*.swo
*~

# ─── ОС ──────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
ehthumbs.db

# ─── Прочее ──────────────────────────────────────────────────────────────────
*.bak
*.bak.*
.claude/settings.local.json

# ─── Инструменты агента ──────────────────────────────────────────────────────
# Скилы Claude Code — стороннее содержимое и дев-инструмент, не код приложения.
# Репозиторий публичный, поэтому в него не кладём.
.claude/skills/
skills-lock.json

# ─── Рабочие фотографии деталей ──────────────────────────────────────────────
# Снимки с борта: съёмка судна и оборудования, в публичном репозитории им не место
photos/
